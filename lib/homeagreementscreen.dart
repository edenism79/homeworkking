import 'package:flutter/material.dart';

class HomeAgreementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('알림장'),
      ),
      body: Center(
        child: Text('알림장 화면입니다.'),
      ),
    );
  }
}
